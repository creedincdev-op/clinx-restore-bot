Archived CLINX panel drafts live here.

- `boost_rewards_panel.py` is the retired `/panel boostrewards` flow.
- `suggestion_panel.py` mirrors the active suggestion panel for safekeeping.

These files are not wired into the bot command tree.
